// Minimal layout for the researcher route group.
// The Navbar in dashboard/page.jsx already provides nav. This wrapper just
// ensures we have a separate compile boundary from (public) and (admin).

export default function ResearcherLayout({ children }) {
  return children;
}
